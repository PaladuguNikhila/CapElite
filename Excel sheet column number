class Solution:
    def titleToNumber(self, columnTitle: str) -> int:
        total=0
        for j in range(len(columnTitle)):
            digit=(ord(columnTitle[j])-ord('A')+1)
            total=total*26+digit
        return total
